{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ctrt.local/schemas/revocation-gated-current-checkpoint-witness-conflict-final.schema.json",
  "title": "Revocation-gated current checkpoint witness conflict final",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "final_id",
    "experiment_run_id",
    "status",
    "current_conflict_adjudicator_revocation_outcome",
    "current_conflict_adjudicator_credential_outcome",
    "conflicting_witness_outcome",
    "current_resolution_status",
    "current_conflict_adjudication_outcome",
    "resolved_current_witness_outcome",
    "current_revocation_outcome",
    "current_credential_outcome",
    "lower_checkpoint_witness_outcome",
    "lower_resolution_status",
    "lower_conflict_adjudication_outcome",
    "lower_predecessor_witness_outcome",
    "inherited_revocation_outcome",
    "inherited_credential_outcome",
    "inherited_checkpoint_witness_outcome",
    "inherited_resolution_status",
    "inherited_adjudication_outcome",
    "terminal_outcome",
    "experiment_id",
    "experiment_version",
    "content_ids",
    "revocation_corpus_ref",
    "predecessor_credential_corpus_ref",
    "revocation_policy_ref",
    "revocation_ledger_ref",
    "revocation_event_refs",
    "adjudication_ref",
    "revocation_decision_ref",
    "credential_final_ref",
    "verified_checks",
    "completed_at"
  ],
  "properties": {
    "final_id": {"type": "string", "minLength": 1},
    "experiment_run_id": {"type": "string", "minLength": 1},
    "status": {"const": "verified"},
    "current_conflict_adjudicator_revocation_outcome": {
      "enum": ["execute", "abstain"]
    },
    "current_conflict_adjudicator_credential_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "conflicting_witness_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "current_resolution_status": {
      "type": ["string", "null"],
      "enum": ["not_required", "pending", "unresolved", "resolved", null]
    },
    "current_conflict_adjudication_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "resolved_current_witness_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "current_revocation_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "current_credential_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "lower_checkpoint_witness_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "lower_resolution_status": {
      "type": ["string", "null"],
      "enum": ["not_required", "pending", "unresolved", "resolved", null]
    },
    "lower_conflict_adjudication_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "lower_predecessor_witness_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "inherited_revocation_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "inherited_credential_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "inherited_checkpoint_witness_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "inherited_resolution_status": {
      "type": ["string", "null"],
      "enum": ["not_required", "pending", "unresolved", "resolved", null]
    },
    "inherited_adjudication_outcome": {
      "type": ["string", "null"],
      "enum": ["execute", "abstain", null]
    },
    "terminal_outcome": {"enum": ["execute", "abstain"]},
    "experiment_id": {"type": "string", "minLength": 1},
    "experiment_version": {"type": "string", "minLength": 1},
    "content_ids": {
      "type": "array",
      "minItems": 2,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    },
    "revocation_corpus_ref": {"$ref": "#/$defs/storedRef"},
    "predecessor_credential_corpus_ref": {"$ref": "#/$defs/versionedRef"},
    "revocation_policy_ref": {"$ref": "#/$defs/storedRef"},
    "revocation_ledger_ref": {"$ref": "#/$defs/storedRef"},
    "revocation_event_refs": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"$ref": "#/$defs/storedRef"}
    },
    "adjudication_ref": {"$ref": "#/$defs/storedRef"},
    "revocation_decision_ref": {"$ref": "#/$defs/storedRef"},
    "credential_final_ref": {
      "anyOf": [
        {"$ref": "#/$defs/storedRef"},
        {"type": "null"}
      ]
    },
    "verified_checks": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    },
    "completed_at": {"type": "string", "format": "date-time"}
  },
  "$defs": {
    "storedRef": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "artifact_id",
        "artifact_hash",
        "canonicalization_version",
        "media_type"
      ],
      "properties": {
        "artifact_id": {"type": "string", "minLength": 1},
        "artifact_hash": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$"
        },
        "canonicalization_version": {
          "const": "ctrt-canonical-json@0.1.0"
        },
        "media_type": {"const": "application/json"}
      }
    },
    "versionedRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["artifact_id", "artifact_version", "artifact_hash"],
      "properties": {
        "artifact_id": {"type": "string", "minLength": 1},
        "artifact_version": {"type": "string", "minLength": 1},
        "artifact_hash": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$"
        }
      }
    }
  }
}